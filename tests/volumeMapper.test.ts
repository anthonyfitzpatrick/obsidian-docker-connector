import type { DockerMountSummary } from "../src/containers/ContainerModels";
import { describe, expect, it } from "vitest"; import { VolumeMapper } from "../src/volumes/VolumeMapper";
const container = (mounts: DockerMountSummary[]) => ({ id: "c", shortId: "c", names: ["web"], displayName: "web", image: "x", createdAt: "", createdTimestamp: 0, state: "running" as const, statusText: "", health: "none" as const, ports: [], mounts, networks: [], labels: {}, hostProfileId: "h", mapperWarnings: [] });
describe("VolumeMapper", () => { it("detects usage only from matching named volume mounts", () => { const volume = VolumeMapper.summary({ Name: "data", Driver: "local", Scope: "local" }, "h", [container([{ type: "volume", name: "data", destination: "/data" }]), container([{ type: "bind", source: "data", destination: "/data" }])]); expect(volume.containersUsingVolume).toBe(1); expect(volume.inUse).toBe(true); }); it("maps safe fallbacks, options and labels", () => { const v = VolumeMapper.summary({ Labels: { app: "web" }, Options: { type: "nfs" } }, "h", []); expect(v.name).toBe("Unnamed volume"); expect(v.labels.app).toBe("web"); expect(v.options.type).toBe("nfs"); }); });
